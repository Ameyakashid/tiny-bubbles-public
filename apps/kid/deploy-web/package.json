{
  "name": "tiny-bubbles-web",
  "version": "1.0.0",
  "private": true,
  "description": "Static server for the Tiny Bubbles web build (Cloud Run).",
  "engines": { "node": ">=20" },
  "dependencies": { "express": "^4.19.2" },
  "scripts": { "start": "node server.js" }
}
