/// <reference types="nativewind/types" />

// Allow side-effect imports of the Tailwind/NativeWind stylesheet
// (e.g. `import "../global.css"` in app/_layout.tsx). Mirrors apps/kid.
declare module "*.css" {}
