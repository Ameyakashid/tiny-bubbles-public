/**
 * packages/shared/src/firestore/index.ts — Firestore contract barrel (w1
 * M1.2). Doc types (§2.3 canonical) + path helpers. Pure types/strings; no
 * SDK import — safe in every workspace including the RN apps.
 */
export * from "./types";
export * from "./paths";
